# AGENTS.md

五連戰線：9×9 戰棋 × 五子棋。Node + WebSocket，前端是原生 HTML/JS，沒有框架。

## 動手前必讀

- **要改介面 → 先讀 [UX-規格.md](UX-規格.md)。** 畫面架構、已知 bug、施做順序、安全考量都在裡面
- 改完一律跑 `npm test`（目前 106 項）

## 不可違反的約束

這些是專案擁有者已經拍板的，**不要重新提案、也不要自行改回**：

1. **數值只有一份，在引擎裡。** 前端不准自己抄 HP／攻擊／炮擊傷害／輪數等任何數值，一律呼叫
   `GameEngine.unitCatalog()` / `artilleryRules()` / `overtimeRules()` / `movementRules()`；
   連線端讀 `state` 裡對應欄位。**已有測試在守這件事，抄了會紅**
2. **UI 不可以跳來跳去。** 會出現／消失的元件必須有固定佔位（例：`#phaseBadge` 那一列固定 22px，
   沒警示時是透明而不是消失）。任何會擠壓其他元件的做法都不接受
3. **不要做連線威脅高亮。** 這是明確排除的設計，理由是會減損發現的樂趣
4. **不要加「位移形成的五連不算」這類特殊例外。** ★★劍斬入與無牌移動造成的五連一律算數
5. **★★盾是 100% 反震。** 不要改成 33%／50% 或其他倍率
6. **使用者輸入（房名、暱稱）一律用 `textContent`，絕不進 `innerHTML`**

## 檔案分工

| 檔案 | 職責 |
|---|---|
| `game_engine.js` | 唯一的規則來源。瀏覽器與 Node 共用 |
| `alpha_ui.js` | 兩端共用的顯示層。**只有文案與 DOM，不含規則計算** |
| `local_client.js` | 單機（`/local`），直接用引擎 |
| `alpha_client.js` | 連線端，狀態來自 server |
| `server.js` | 房間、WebSocket、對局仲裁 |
| `tests/` | 規格書。每條規則都有對應測試 |
| `micro/` | 平衡模擬腳本，可直接 `node micro/xxx.js` 重跑 |

## 尚未決定的事——不要自己選答案，問擁有者

- **牌組大小**（目前 25 張：劍9/盾9/槍7）。相關數字：場上綁住的牌固定約 10.5 張，
  所以縮牌組是整包從抽牌區扣
- **加賽是否要有輪數上限**（目前沒有；長度中位數 3 輪、p90 是 6 輪）
- **斷線超時**（目前完全沒有，會一直等）
- **加賽緩衝 3 輪 vs 消極判負 3 輪的交互**（見 UX-規格.md 8.6）

## ⚠️ 一個踩過的坑

曾有一份由 AI 產生的「不要動」清單被後續 AI 當成擁有者的意向，因而擋掉了擁有者自己提出的
設計。**規格與擁有者當下的說法衝突時，以擁有者為準，直接問他。**
