# 五連戰線：介面規格

## 〇、原則

**一個畫面只回答一個問題。** 這個畫面在幹嘛，就只顯示那件事必要的東西；不同的事情分到不同的頁面。

目前的問題不是「某個元件做壞了」，而是**大廳、對局、結算三件事共用同一個畫面**。每一塊只分到一點空間，結果就是每一塊都看不清楚。修法不是繼續往側欄塞，是把它們拆開。

> 參考做法：chess.com、圍棋 App、Advance Wars、爐石。共同點是——對局畫面上**棋盤／戰場佔主體**，其餘資訊是環繞的窄帶；房間與結算都是獨立畫面或全螢幕覆蓋層。

---

## 一、畫面架構

```
大廳頁          房間列表、建立房間、對手狀態
   ↓ 兩人就緒
對局頁          棋盤為主角；上下兩條玩家資訊帶
   ↓ 分出勝負
結算覆蓋層      誰贏、怎麼贏、再來一局 / 離開
```

現在這三者全擠在一頁，是所有問題的根源。

---

## 二、大廳頁

**回答：有哪些房間可以進，或我要開一間。**

### 🔴 房號機制要換掉

現在是「建立房間 → 拿到 6 碼亂碼 → 用別的管道告訴對方 → 對方手動輸入」。兩個人開一局，中間卡了一次複製貼上，而且**別人開了房，你在畫面上完全看不到**。

### 房間列表

大廳主體是**等待中的房間列表**，有人開房就出現，自動更新。每張卡：

| 欄位 | 說明 |
|---|---|
| **房名** | 建立者自訂；沒填就預設「〇〇 的房間」 |
| 建立者 | 暱稱或代稱 |
| 狀態 | 等待中 / 對局中（對局中不可加入） |
| 🔒 | 有設密碼才顯示 |
| 建立時間 | 相對時間，例如「2 分鐘前」 |

**點卡片就加入**，有密碼才跳輸入框。

### 建立房間

一個小表單，不跳頁：**房名**（選填）、**密碼**（選填，預設不設）。建立後留在大廳等對手，顯示「離開房間」。

### 保留但降級

- **用房號直接加入**保留在次要位置，適合不公開的房或列表太長時
- 入座後，「建立房間」「加入房間」「房間列表」全部**消失**，改顯示自己的房間卡與「離開房間」

### 其他

- **對手狀態**：未加入 / 已連線 / 已斷線，用頭像或色塊，不要只有文字
- 規則說明入口、單機模式入口

不該出現：棋盤、手牌、戰報、炮擊資源。

### 伺服器要補的

現有 `broadcastRoom()` 只送給房內兩個座位，**大廳裡還沒入座的人收不到任何東西**，所以要另外做：

- `room` 新增 `name`、`password`（可為 `null`）、`createdBy`
- **新增大廳頻道**：未入座的連線訂閱房間列表，房間建立／加入／關閉時廣播
- 列表只列 `status === "waiting"` 的房間
- `join_room` 接受 `password` 並驗證，密碼錯誤要回明確錯誤
- 🔴 **密碼絕對不能送到前端**，房間卡只帶 `hasPassword: true/false`
- 🔴 **房間列表不要洩漏 `token`**（那是重連憑證，洩漏等於別人可以佔你的座位）

測試要涵蓋：未入座者收得到列表且不含密碼與 token／密碼錯誤無法加入／對局中的房間不出現在列表／建立者離開後房間消失。

---

## 三、對局頁

**回答：現在什麼局面，輪到誰，我可以做什麼。**

```
┌─────────────────────────────────────┐
│  對手資訊帶  ← 頭像／P幾／手牌數／剩餘炮  │
├─────────────────────────────────────┤
│                                     │
│            棋  盤（主角）             │
│                                     │
├─────────────────────────────────────┤
│  自己資訊帶  ← 同上，輪到你時整條發亮    │
├─────────────────────────────────────┤
│  手牌                                │
└─────────────────────────────────────┘
```

### 🔴 輪到誰

現在只有側欄一行小字，跟「第 3 輪」同樣字級。**這是每一秒都要知道的事，必須不可能錯過。** 至少做到兩項：

- 輪到你時，**你的資訊帶整條發亮／變色**
- 棋盤外框跟著換色（P1 紅、P2 藍）
- 不是你的回合時，手牌整體降低亮度

### 🔴 明確的「結束回合」

現在部署完就自動換人。問題不在系統——炮擊放在部署前或後，戰術上幾乎沒有差別——**問題在人**：玩家常常是放了棋才想到要放炮，而整場只有 2 發，錯過一次很挫折。

- 部署（或移動）後**不自動結束回合**，改由玩家按「結束回合」
- 一併放寬引擎的「炮擊只能在部署前使用」，否則那顆按鈕沒事可做
- 🔴 **放完棋之後，「結束回合」必須主動吸引注意**——發光、脈動、或整條玩家帶變色。只是靜靜擺著，很多人會忘記按而卡住整局。**這是這項改動的成敗關鍵，不是裝飾**
- 會動到 `game_engine.js` 的 `deploy()` / `move()` / `artillery()`、伺服器與兩個客戶端，**要跑測試**

### 🔴 已知未解 bug：輪到自己但炮擊鈕是灰的

回報過兩次。現場觀察到的兩個事實把範圍縮得很小：

1. **只有炮擊不能點，手牌可以點。** 手牌的 `disabled` 是 `!ownTurn()`，所以 `ownTurn()` 當時是 **true**——排除了「對手斷線」與「`pendingRequest` 卡住」，那兩者會讓所有按鈕一起變灰
2. **在棋盤上點一顆已有的棋子之後就恢復正常。** 那條路徑只做了 `notice = "…"; render();`，**沒有送任何東西給伺服器**

> **結論：伺服器狀態是對的，是客戶端畫出來的按鈕過期了。** 某個狀態變化之後漏掉 `render()`，一次純本地重繪就會修好。
> 剩下的嫌疑只有三個：`artillery <= 0`（但按鈕會寫「剩 0 發」）、`artilleryUsedThisTurn`、`deploymentCommitted`。

**不要再用推理找答案，先讓畫面自己說**（見下一項），下次一遇到就直接看得到是哪個條件擋住。

### 🔴 按鈕變灰時必須說明原因

炮擊鈕的 `disabled` 把四個原因塞進一個布林值，其中 `ownTurn()` 本身又是五個條件的 AND：

```js
artilleryButton.disabled = !ownTurn()          // 連線／有state／無等待中請求／未結束／對手在線／輪到我
  || state.artillery[selfPid] <= 0             // 炮用完了
  || state.artilleryUsedThisTurn               // 本回合用過了
  || state.deploymentCommitted;                // 已經部署
```

- **每顆會變灰的按鈕都要能說出理由**，寫在按鈕上或旁邊：「不是你的回合」「對手已斷線」「本場炮擊已用完」「本回合已使用」「等待伺服器回應」
- 引擎的錯誤訊息已經寫得很清楚（`炮擊只能在部署前使用` 等），**前端應該顯示它們，而不是自己變灰了事**
- `pendingRequest` 要有逾時保護：超過 N 秒沒收到 `state` 就自動解除並提示重試

### 其他

- **炮擊鈕**放在自己的資訊帶上，顯示剩餘發數
- **棄賽**移出頂欄，放進次要位置（例如右上選單），但要點得到
- **戰鬥日誌**改成可收合抽屜，預設收起，不要常駐佔位
- 手牌只顯示手牌；兵種詳情用滑過／點選浮出的大卡（已完成）

不該出現：房號、建立/加入房間、卡片守恆統計、完整戰報。

---

## 四、結算覆蓋層

**回答：誰贏了，為什麼，接下來做什麼。**

### 🔴 目前最荒謬的一點

分出勝負後，結果只是側欄一行小字，字級跟「第 3 輪｜P1 行動」一樣。**玩家得自己推敲誰贏了。**

要求：**全螢幕覆蓋層，不可能錯過。**

- **大字寫明結果**：`P1 獲勝` / `平手` / `消極對局：雙方棄賽`
- **寫明原因**（引擎已提供 `endReason`，直接讀）：

  | `endReason` | 顯示 |
  |---|---|
  | `five_line` | 五連獲勝（加賽中要標明「加賽」） |
  | `passivity_forfeit` | 雙方連續 3 輪未交戰，消極對局判雙敗 |
  | 平手 | 雙方同輪五連 |

- **在棋盤上標出致勝的那五顆**（`finalFive` 已有資料），定格顯示最終盤面
- 按鈕：**再來一局** / 離開房間 / 看戰報
- 戰報（輪數、炮擊使用、卡片統計）收進「看戰報」，不要一開始就倒出來

---

## 五、交戰演出

**這是理解遊戲的關鍵，不是裝飾。**

目前雙方落子後，盤面直接「跳」到結算後的樣子。玩家看不到誰打了誰、誰擋了誰、誰反震——**等於整套相剋、護衛、斬入、反震系統對玩家不存在。**

分段播放，每段之間短暫停頓：

1. **主攻擊**：畫出攻擊線，被護衛轉移的要看得出來轉去哪
2. **傷害與陣亡**：傷害數字浮出，陣亡單位淡出
3. **★★劍斬入**：**棋子移動要有軌跡動畫**——不演出來會像 bug
4. **★★盾反震**：反向的傷害數字

需要「跳過演出」選項給熟練玩家。

---

## 六、建議施做順序

### 🔴 階段零：動手前先合併分支

**現在有三套規則同時存在**，這份規格是照 `no-cards-move` 寫的：

| 分支 | 沒牌可放時 |
|---|---|
| `main` | 卡住不結束（洞還開著） |
| `codex/alpha-core-v1-fixes` | 判輸 |
| **`no-cards-move`** | **移動一格**（本規格採用） |

如果從 `main` 開始做 UI：

- 第四節叫你讀 `endReason` → **`main` 上沒有這個欄位**，會拿到 `undefined`
- `movementRules()`、`canAct()`、`legalMoves()` 也都不存在
- 規則視窗的文案在三條分支上是三種說法

**所以第一件事是把 `no-cards-move` 合進 `main`**（它已包含 `codex/alpha-core-v1-fixes` 的全部內容，合這支等於兩支都合）。不先做這個，結算畫面一合併就要重寫。

---

之後分五個階段。**階段一全部獨立、不需重構，可以馬上開始，也可以並行。**

### 階段一：快速止血（不動版面）

| # | 項目 | 為什麼排在最前 |
|---|---|---|
| 1 | **輪到誰**：大字＋顏色＋資訊帶發亮 | 最基本，看不出來就沒法玩 |
| 2 | **結算覆蓋層**：誰贏、為什麼、標出致勝五顆 | 一局結束卻不知道誰贏，最傷體驗；引擎資料（`endReason`、`finalFive`）都現成 |
| 3 | **按鈕說明變灰原因** | 成本極低，而且這是抓那個炮擊 bug 的**手段**——做完就能自己診斷 |

### 階段二：交戰演出

| # | 項目 | 為什麼 |
|---|---|---|
| 4 | 主攻擊／傷害／斬入軌跡／反震，分段播放 | 單項價值最高。沒有它，整套戰鬥系統對玩家等於不存在。**不需要等版面重構**，動的是棋盤本身 |

### 階段三：版面重構

| # | 項目 | 為什麼 |
|---|---|---|
| 5 | 拆成大廳／對局／結算三個畫面 | 拆完才有空間放後面的東西 |
| 6 | 大廳房間列表 ＋ 選填密碼（含伺服器大廳頻道） | 跟 5 一起做。工程量主要在伺服器端的大廳頻道，不只是 UI |

### 階段四：回合控制

| # | 項目 | 為什麼排在重構之後 |
|---|---|---|
| 7 | 明確「結束回合」＋ 放完棋發光 ＋ 放寬炮擊時機 | 它要動引擎、伺服器、兩個客戶端。**若在重構前做，UI 會做兩次**。而且發光提示要放在新的玩家資訊帶上 |

### 階段五：收尾

| # | 項目 |
|---|---|
| 8 | 戰鬥日誌改抽屜 |
| 9 | 棄賽鈕移位 |

---

## 七、你回報的問題 → 對應項目

| 回報 | 對應 | 狀態 |
|---|---|---|
| 不知道輪到誰 | 階段一 #1 | 待做 |
| 分出勝負後不知道誰贏 | 階段一 #2 | 待做 |
| 炮擊點不下去、也不知道為什麼 | 階段一 #3 | 待做（診斷手段） |
| 不知道戰鬥怎麼得到這結果 | 階段二 #4 | 待做 |
| 什麼都擠在一頁 | 階段三 #5 | 待做 |
| 房間機制麻煩、看不到別人開的房 | 階段三 #6 | 待做 |
| 放了棋才想到要放炮 | 階段四 #7 | 待做 |
| 戰鬥日誌沒人看 | 階段五 #8 | 待做 |
| 棄賽鈕很難注意到 | 階段五 #9 | 待做 |
| 房間問題導致卡死 | — | ✅ 已修（`90dc36a`） |
| 結束後無法再開一局 | — | ✅ 引擎與伺服器已完成，UI 待整合進階段一 #2 |

---

## 八、安全與長期考量

### 8.1 身分：現在只要暱稱，不要做帳號

開房間確實需要「誰開的」，但**那只需要一個暱稱**。註冊系統要處理密碼雜湊、信箱驗證、忘記密碼、帳號救援、速率限制——換來的是跨裝置身分、排行榜、好友、防冒名，**這四個現在都不需要**。

- **現在**：暱稱存 `localStorage`，房間卡顯示它。零後端、零安全風險
- 🔴 **但有一個現在就要做對的架構決定**：**伺服器永遠不要信任前端送來的暱稱當作身分**。身分一律是伺服器發的 token（現有的 `room.players[pid].token` 就是），暱稱只是顯示用的標籤

這樣以後要加帳號，只是把「匿名 token」換成「帳號 token」，遊戲邏輯一行都不用改。**反之，若現在把暱稱當識別穿進遊戲邏輯，之後加帳號就是大手術。**

### 8.2 🔴 房名與暱稱的 XSS

**這是本次新功能唯一會引進的真實漏洞。** 房名是使用者輸入、且要顯示在別人畫面上。

- **房名、暱稱一律用 `textContent` 塞入，絕對不准進 `innerHTML`**
- 房名長度要設上限（例如 24 字）並過濾控制字元
- 若房間卡是用樣板字串組 HTML，必須先跳脫

### 8.3 公開之前要補的三件

| 項目 | 現況 | 何時要做 |
|---|---|---|
| **開房洪水** | `rooms` 是記憶體 Map，30 分鐘才過期。狂送 `create_room` 可以吃光記憶體 | 公開時 |
| **密碼暴力破解** | 加密碼後 `join_room` 沒有次數限制 | **加密碼時就要做** |
| **token 外洩** | 重連 token 等於座位所有權，房間列表絕對不能帶到它 | 做列表時 |

已經做對的，不要改壞：`maxPayload: 16KB`、戰報檔名有過濾路徑穿越、伺服器信任的是 socket 綁定的 session 而不是前端宣稱的身分。

### 8.4 🔴 逾時機制（跟「結束回合」綁在一起做）

規格第三節提議「明確結束回合」，但**沒有處理「玩家永遠不按」**。那會造成：

- 對局永遠掛著，對手只能離開
- **這是可以故意惡搞的手段**——按著不放，逼對方棄賽

而且**目前斷線也沒有超時**，對手斷線後你會一直等。所以會有兩種「讓對局永遠不結束」的方法。

**做「結束回合」的同時，一併加逾時**，兩者共用同一套機制：

- 回合逾時（例如 60 秒）→ 自動結束回合
- 斷線逾時（例如 120 秒）→ 判離場
- 逾時秒數由引擎提供給 UI，不要在前端寫死

### 8.5 Steam 與手機：不要現在做，但兩個決定要現在下

技術上 HTML/JS + Node 這套上 Steam 走 Electron、上手機走 Capacitor 或 PWA，**不需要重寫**，所以不急。但兩件事現在不決定、之後會很貴：

1. 🔴 **版面直接照手機直向設計。** 第三節那個「上下玩家帶 + 中間棋盤」的結構本來就手機友善，直向手機用它剛好。**現在順手做，比之後 retrofit 便宜十倍**
2. 🔴 **不要有「只能 hover 才看得到」的資訊。** 觸控沒有 hover——第三節的「按鈕說明變灰原因」如果做成 tooltip，手機永遠看不到，那條說明要**直接寫在畫面上**。兵種大卡已經用 `matchMedia("(hover: none)")` 處理過，新做的東西照同樣原則

### 8.6 待你決定的規則交互

**加賽期間若雙方零交戰，消極判負會在第 3 輪先觸發，比衰減開始的第 4 輪早一輪。** 所以「進了加賽卻不打」＝雙敗，10% 衰減永遠登不了場。

這是兩條規則各自照規格運作的結果，已寫在 `tests/alpha_core.test.js` 的測試 39。若要讓衰減有機會出場，把加賽緩衝從 3 輪改成 2 輪即可。**目前維持現狀，等你決定。**

---

## 九、給實作者的注意事項

- **數值一律向引擎要**，前端不要自己抄。`GameEngine.unitCatalog()` / `artilleryRules()` / `overtimeRules()` / `movementRules()`；連線端讀 `state` 裡對應欄位。已有測試在守
- **UI 不可以跳來跳去。** 會出現／消失的元件要用固定佔位（例如 `#phaseBadge` 那一列固定 22px，沒警示時透明而不是消失）
- **連線提示（威脅高亮）不要做。** 這是明確排除的設計——那條線有時連自己都沒發現，全顯示出來會少掉樂趣
- **使用者輸入一律 `textContent`**（見 8.2）
- 改完跑 `npm test`
